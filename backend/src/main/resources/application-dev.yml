spring:
  datasource:
    url: jdbc:h2:mem:bjjacademy;MODE=PostgreSQL;DATABASE_TO_LOWER=TRUE
    username: sa
    password:
  h2:
    console:
      enabled: true
      path: /h2-console

app:
  security:
    jwt-secret: ${JWT_SECRET:}
    token-duration: PT8H
    admin-email: ${ADMIN_INITIAL_EMAIL:admin@fenix.local}
    create-dev-admin: true
